"use client";
import { ImagePlus, X } from "lucide-react";
export default function ClientLogoUploader({ value, onChange, error }: { value: string; onChange: (value: string) => void; error?: string }) {
  const onFile = (file?: File) => { if (!file) return; if (!['image/png','image/jpeg','image/svg+xml'].includes(file.type) || file.size > 2 * 1024 * 1024) return; const reader = new FileReader(); reader.onload = () => onChange(String(reader.result)); reader.readAsDataURL(file); };
  return <div><p className="text-sm font-bold">Company logo <span className="text-rose-500">*</span></p><label className="mt-2 flex min-h-32 cursor-pointer items-center justify-center rounded-xl border border-dashed border-slate-300 bg-slate-50 p-4 text-center transition hover:border-primary dark:border-white/20 dark:bg-white/[0.03]"><input className="sr-only" type="file" accept="image/png,image/jpeg,image/svg+xml" onChange={(event) => onFile(event.target.files?.[0])} />{value ? <img src={value} alt="Logo preview" className="max-h-24 max-w-full object-contain" /> : <span className="text-sm text-slate-500"><ImagePlus className="mx-auto mb-2 h-5 w-5" />Drop PNG, JPG, or SVG here<br />Maximum size 2 MB</span>}</label>{value && <button type="button" onClick={() => onChange("")} className="mt-2 inline-flex items-center gap-1 text-xs font-bold text-rose-500"><X className="h-3 w-3" />Remove logo</button>}{error && <p className="mt-1 text-xs text-rose-500">{error}</p>}</div>;
}
