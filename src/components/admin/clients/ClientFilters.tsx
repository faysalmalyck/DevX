"use client";
export type ClientFiltersState = { search: string; status: string; featured: string; industry: string; country: string; sort: string; };
export default function ClientFilters({ value, onChange }: { value: ClientFiltersState; onChange: (value: ClientFiltersState) => void }) { const field = (key: keyof ClientFiltersState, label: string, options?: string[]) => <label className="text-xs font-bold text-slate-500">{label}{options ? <select value={value[key]} onChange={(e) => onChange({ ...value, [key]: e.target.value })} className="mt-1 block w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-base text-slate-800 dark:border-white/10 dark:bg-white/5 dark:text-white">{options.map((option) => <option key={option} value={option}>{option}</option>)}</select> : <input value={value[key]} onChange={(e) => onChange({ ...value, [key]: e.target.value })} placeholder="Search companies" className="mt-1 block w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-base text-slate-800 dark:border-white/10 dark:bg-white/5 dark:text-white" />}</label>; return <div className="grid gap-3 rounded-lg border border-slate-200 bg-white p-4 dark:border-white/10 dark:bg-white/[0.04] md:grid-cols-6">{field("search", "Search")}{field("status", "Status", ["All statuses", "ACTIVE", "HIDDEN"])}{field("featured", "Featured", ["All clients", "Featured", "Not featured"])}{field("industry", "Industry")}{field("country", "Country")}{field("sort", "Sort", ["Display order", "Company name", "Newest"] )}</div>; }
