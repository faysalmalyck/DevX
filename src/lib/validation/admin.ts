import type { Administrator } from "@/lib/permissions/rbac";
export function validateAdministrator(admin: Administrator, admins: Administrator[], password?: string, confirmPassword?: string) { const errors: Record<string, string> = {}; if (!admin.firstName.trim()) errors.firstName = "Full name is required."; if (!/^\S+@\S+\.\S+$/.test(admin.email)) errors.email = "Enter a valid email address."; if (!admin.username.trim()) errors.username = "Username is required."; if (admins.some((item) => item.id !== admin.id && item.email.toLowerCase() === admin.email.toLowerCase())) errors.email = "This email is already in use."; if (admins.some((item) => item.id !== admin.id && item.username.toLowerCase() === admin.username.toLowerCase())) errors.username = "This username is already in use."; if (password && (password.length < 12 || !/[A-Z]/.test(password) || !/[a-z]/.test(password) || !/\d/.test(password))) errors.password = "Use 12+ characters with upper, lower, and number."; if (password !== confirmPassword) errors.confirmPassword = "Passwords do not match."; return errors; }
